{
  "name": "app",
  "private": true,
  "version": "1.0.30",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint",
    "preview": "vite preview",
    "test:insight": "node test/insightEngine.test.js",
    "test": "npm run verify && npm run test:insight",
    "verify": "node -e \"import('./src/utils/notificationProviders.js').then(m => { const res = m.runParserTests(); console.log('===================================='); console.log('Cassiel Notification Auto Tracker Verification'); console.log('Total Tests Run: ' + res.length); const passed = res.filter(t => t.passed).length; const failed = res.filter(t => !t.passed).length; console.log('Passed: ' + passed + ' | Failed: ' + failed); console.log('===================================='); if (failed > 0) { console.error('FAILED TESTS:'); res.filter(t => !t.passed).forEach(t => console.error(' - ' + t.name + ' => expected: ' + t.expected + ', actual: ' + t.actual)); process.exit(1); } else { console.log('ALL NOTIFICATION TRACKER TESTS PASSED SUCCESSFULLY! (100%)'); } })\"",
    "postinstall": "node scripts/patch-speech-spm.js"
  },
  "dependencies": {
    "@capacitor-community/speech-recognition": "^7.0.1",
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/local-notifications": "^8.2.1",
    "@capacitor/share": "^8.0.1",
    "@capawesome/capacitor-badge": "^8.0.2",
    "@capgo/capacitor-native-biometric": "^8.6.6",
    "firebase": "^12.17.1",
    "lucide-react": "^1.30.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "oxlint": "^1.75.0",
    "vite": "^8.2.0"
  }
}
